/** Virtual economy & market catalog (DB single source of truth). */
export {
  getMarketState,
  purchaseMarketItem,
  applyMarketAura,
  type MarketItemDTO,
  type MarketStateDTO,
} from "@/lib/services/market.service";
